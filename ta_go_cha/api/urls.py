from django.urls import path
# from views import 

app_name = "ta_go_cha"
urlpatterns = [
    # path( "", HomeView.as_view() , name="home"),
    # # path( "targets/", TargetListView.as_view() , name="targets-list"),
    # path( "target/<slug:name>", SubTargetLitView.as_view(), name="sub-targets-list"),
    # path( "targets/create/", TargetCreateView.as_view(), name="target-create" ),
    # path( "this-week/", this_week ),

]
